name: spotlight_player
description: "Music Player Spotlight Effect."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  collection: ^1.19.1
  audioplayers: ^6.5.0
  window_size: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/audios/HeroesTonight.mp3
    - assets/images/HeroesTonight.jpg
